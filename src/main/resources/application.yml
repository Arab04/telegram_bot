bot:
  name:
  token:
  ownerId:

spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/postgres
    username: nova
    password: nova
    driverClassName: org.postgresql.Driver